<% provide(:title, @user.name) %>
<div class="row">
    <aside class="span4">
        <section>
            <h1> 
                <%= gravatar_for @user %>
                <span><%= @user.name %></span>
                <span><%= @user.email %></span> 
            </h1>
        </section>
    </aside>
</div>