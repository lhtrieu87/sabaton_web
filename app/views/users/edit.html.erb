<% provide(:title, "Edit User")%> <h1>Update Profile</h1>
<div class="block4 centralized">
    <%= form_for(@user) do |f| %>
    <%= render 'layouts/user_form_fields', f: f %>
    <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
    <%= gravatar_for @user %>
    <a href="http://gravatar.com/emails">Change</a>
</div>

