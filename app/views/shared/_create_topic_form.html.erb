<section>
    <%= form_for(@topic) do |f| %>
        <%= render 'shared/error_messages', object: f.object %>
        <div class="field">
            <%= f.text_area :content, placeholder: "Create new topic..." %>
        </div>
        <%= f.submit "Create", class: "btn btn-primary" %>
    <% end %>
</section>